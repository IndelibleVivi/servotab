interface:
  display_name: "License Boundary"
  short_description: "Choose repository licenses from goals and rights"
  default_prompt: "Use $license-boundary to inspect this project's permission goals and rights evidence. Recommend one best-fit license and scope in plain language, with only material alternatives. Treat the exact choice as the user's; edit only after explicit selection and file-edit authorization."
policy:
  products:
    - CODEX
  allow_implicit_invocation: true
