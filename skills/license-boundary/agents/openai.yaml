interface:
  display_name: "License Boundary"
  short_description: "Choose repository licenses from goals and rights"
  default_prompt: "Use $license-boundary to inspect this project's permission goals and rights evidence, then recommend the smallest correct licensing boundary. Edit files only when the user's request authorizes it."
policy:
  products:
    - CODEX
  allow_implicit_invocation: true
