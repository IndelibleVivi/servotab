interface:
  display_name: "Softpowers"
  short_description: "Quiet risk-scaled routing for repository work"
  default_prompt: "Use $softpowers to handle this repository task with the smallest useful engineering method and fresh, risk-matched verification."
policy:
  products:
    - CODEX
  allow_implicit_invocation: true
