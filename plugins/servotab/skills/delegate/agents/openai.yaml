interface:
  display_name: "Delegate"
  short_description: "Choose responsibility before deep work"
  default_prompt: "Use $delegate to choose responsibility before deep work, define bounded worker lanes, dispatch or reuse the fewest useful agents with explicit authority, and verify their returns before integration."
  brand_color: "#315EFB"
  icon_small: "./assets/icon-400.png"
  icon_large: "./assets/icon.svg"
policy:
  allow_implicit_invocation: false
