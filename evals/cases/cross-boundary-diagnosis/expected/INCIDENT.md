# Diagnostic outcome (fixture reference, not a target-agent receipt)

The local health probe bypasses client admission. Ordinary desktop load does not stress the budget, but a client-origin eight-request workload isolates the shared admission mechanism under clean access. The earlier identifier-specific trial lacks proof of intervention and comparable conditions; it does not refute the recorded admission violation.

Comparable client/clean probes before and after the policy repair yield admission_dropped 4 -> 0 and delivered 4 -> 8. Noisy access still produces late delivery after admission is repaired; this remains a distinct observation. The permanent exemption follows active relay ownership plus the tag and client entry, retaining untagged and disabled-relay limits. The regression covers those negative paths.

Only this model's admission mechanism and delivery are verified. Current named-handheld experience and live lifecycle behavior remain unverified; the simulator cannot close them. A semantic reviewer must consult the actual attempt's commands and outputs, not treat this reference prose as experiment evidence.
