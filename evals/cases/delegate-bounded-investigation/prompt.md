`SYSTEM.md` bounds a failure in the nightly ingest worker: `queue_worker.py` loses items, the focused test is intermittently red, and `evidence/nightly.log` is noisy. The root cause is not yet localized, but the failure, expected behavior, entry evidence, return contract, and verification are already fixed.

The investigation is noisy and will hold the current thread's attention for a long stretch, and a second copy of the repair must not grow beside the real one. Settle who owns this work before going deep, then carry it out. Repair `queue_worker.py` and write `LANE_REPORT.md` recording the lane outcome, the responsibility and staffing that were chosen, and the verification actually run. Leave every other file unchanged, keep the change inside `SYSTEM.md`'s bounds, and do not add scope.
