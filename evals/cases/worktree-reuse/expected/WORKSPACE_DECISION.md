# Workspace decision

Resume host-managed `task-42` through the supported host action: fresh inventory reconciles the ambiguous create result, its branch is `feature/task-42` at B, and the required parser.py prerequisite and passing baseline are observed. Do not retry creation or copy the source's unrelated changes. The read-only reviewer needs no additional checkout. Leave `old-task-42` alone: its name does not establish task identity or disposal authority.

Before actual resume, refresh the task ownership and prerequisite observations. These are synthetic observations; no host action was executed here. A manual add at B would miss the dirty prerequisite, so it cannot substitute for this checked workspace without an authorized transfer.
