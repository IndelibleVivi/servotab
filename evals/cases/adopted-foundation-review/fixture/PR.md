# Proposed implementation

The PR adds only the adopted shared contract harness and its deterministic fixtures. It does not implement an adapter, widen the protocol, or reorder work beyond the adopted foundation-first programme.
