# Shared contract foundation

Create one shared contract harness before implementing either planned adapter so both adapters inherit the same protocol behavior and fixtures.

This proposal was originally agent-authored and had no authority until repository governance explicitly adopted it.
