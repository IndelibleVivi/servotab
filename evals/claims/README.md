# Claims

This directory belongs to the Servotab subject pack. Keep project-specific
behavior claims here; the optional standalone Field Lab companion owns the
generic execution contract, not Servotab's substantive decisions.
